---
import type { Language } from "../i18n/types";
import { getTranslations, getAlternateLanguage, getLanguagePath } from "../i18n/utils";

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = getTranslations(lang);
const alternate = getAlternateLanguage(lang);
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="wrap inner">
    <p class="line">
      <span>Ronaldo Scotti</span>
      <span class="dot">·</span>
      <span>Florianópolis, {lang === "en" ? "Brazil" : "Brasil"}</span>
      <span class="dot">·</span>
      <span>{year}</span>
    </p>

    <p class="line no-print">
      <a href={`mailto:${t.contact.email}`}>{lang === "en" ? "Email" : "E-mail"}</a>
      <a href="https://linkedin.com/in/ronaldoscottis" rel="noopener">LinkedIn</a>
      <a href="https://github.com/ronaldoscotti" rel="noopener">GitHub</a>
      <a href={getLanguagePath(alternate)} data-lang-choice={alternate === "en" ? "en" : "pt"}>
        {alternate === "en" ? "English" : "Português"}
      </a>
    </p>

    <p class="motto">{t.footer.motto}</p>
  </div>
</footer>

<style>
  /* Deeper than the page, so the document closes on a darker note instead of
     just running out. */
  .site-footer {
    background: var(--color-paper-deep);
    border-top: 1px solid var(--color-rule);
    padding-block: 2.75rem 3.5rem;
  }

  .inner {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .line {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.03em;
    color: var(--color-ink-faint);
  }

  .line a {
    color: var(--color-ink-faint);
    text-decoration: none;
  }

  .line a:hover {
    color: var(--color-accent-deep);
    text-decoration: underline;
  }

  .dot {
    opacity: 0.5;
  }

  .motto {
    margin-top: 0.75rem;
    font-family: var(--font-display);
    font-style: italic;
    font-size: 0.9375rem;
    color: var(--color-accent);
  }
</style>
